import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const CameraScreen = () => <Text>CameraScreen</Text>

const styles = StyleSheet.create({})

export default CameraScreen
