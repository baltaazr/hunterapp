import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const AccountScreen = () => <Text>AccountScreen</Text>

const styles = StyleSheet.create({})

export default AccountScreen
