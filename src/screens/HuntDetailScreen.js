import React from 'react'
import { View, StyleSheet, Text } from 'react-native'

const HuntDetailScreen = () => <Text>HuntDetailScreen</Text>

const styles = StyleSheet.create({})

export default HuntDetailScreen
